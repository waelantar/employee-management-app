export interface Employee {
    id: number;
  age: number;
  dob: string | null;  
  email: string;
  salary: number;  
  address: string;
  imageUrl: string;  
  lastName: string;
  firstName: string;
  contactNumber: string;
  }